/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package com.eren.radiomode.web.util;

/**
 *
 * @author work
 */
public class QueryConstants {
    public static String updatePasswordByUserName = "update User u SET u.password = ? WHERE u.userName = ?";

    public static String updateListenHistory = "update ListenHistory h SET h.listenPercent = ?," +
            "h.volumePercent = ?,h.listenDate= ?,h.status = ? WHERE h.id = ?";
}
